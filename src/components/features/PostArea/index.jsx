import "./module.css";

function PostArea({ children }) {
  // const { comments, currentUser } = appData;
  // const replyLevel = 0;
  return <div className="post-area-container">{children}</div>;
}

export default PostArea;
